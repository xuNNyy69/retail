version: "2.0"
author: xuNNyy
patch: 12.1
spec: 103
name: xuNNyy_druid_feral
entry: main

config:
  use_trinkets:
    label: "Use Items"
    type: checkbox
    default: true
  use_potions:
    label: "Use Potions"
    type: checkbox
    default: true
  use_defensives:
    label: "Use Defensives"
    type: checkbox
    default: true
  use_interrupts:
    label: "Use Interrupts"
    type: checkbox
    default: true
  use_cooldowns:
    label: "Use Cooldowns"
    type: checkbox
    default: true
  aoe_threshold:
    label: "AoE Threshold"
    type: slider
    min: 2
    max: 8
    default: 3
  use_dispel:
    label: "Use Dispel/Soothe"
    type: checkbox
    default: true

variables:
  # Hero-tree routing helpers
  is_dotc: talent.ravage
  is_ws: talent.wildstalkers_power

  # Berserk covers both Berserk and Incarnation: King of the Jungle
  berserk_up: "buff.berserk.up|buff.incarnation_avatar_of_ashamane.up"
  tigers_fury_up: buff.tigers_fury.up
  # Energy thresholds
  energy_low: energy<=50
  # Energy pooling for Ferocious Bite bonus damage (consumes up to 25 extra)
  energy_fb: energy>=50
  # Combo point thresholds
  cp_high: combo_points>=5
  cp_low: combo_points<=2
  # DoT refresh windows
  rip_pandemic: dot.rip.refreshable
  rake_refreshable: dot.rake.refreshable
  # Apex Predator proc (free instant Ferocious Bite)
  apex_predator_up: buff.apex_predators_craving.up
  # Sudden Ambush proc (stealthed Rake from combat)
  sudden_ambush_up: buff.sudden_ambush.up
  # Sabertooth: FB extends Rip — less manual Rip refresh needed
  has_sabertooth: talent.sabertooth
  # Combat gate for items/CDs
  combat_gate: "player.combat&(target.time_to_die>12|active_enemies>=3)"
  # AoE routing
  want_aoe: "state.aoe&active_enemies>=config.aoe_threshold"

lists:
  interrupts:
  - skull_bash,if=target.casting.interruptible
  - skull_bash.mouseover,if=mouseover.casting.interruptible
  - skull_bash.focus,if=focus.casting.interruptible

  defensives:
  - barkskin,if=health.pct<=40,use_off_gcd=1
  - survival_instincts,if=health.pct<=30

  dispel:
  - soothe,if=target.purgeable.enrage
  - soothe.mouseover,if=mouseover.purgeable.enrage
  - remove_corruption,if=player.has_curse|player.has_poison

  racials:
  - berserking,use_off_gcd=1,if=var.berserk_up
  - blood_fury,use_off_gcd=1,if=var.berserk_up
  - fireblood,use_off_gcd=1,if=var.berserk_up
  - ancestral_call,use_off_gcd=1,if=var.berserk_up
  - lights_judgment,if=!var.berserk_up
  - bag_of_tricks,if=!var.berserk_up

  items:
  - trinket1,use_off_gcd=1,if=config.use_trinkets&(var.berserk_up|target.time_to_die<20)&var.combat_gate
  - trinket2,use_off_gcd=1,if=config.use_trinkets&(var.berserk_up|target.time_to_die<20)&var.combat_gate
  - potion,if=config.use_potions&(var.berserk_up|target.time_to_die<20)&var.combat_gate,use_off_gcd=1

  # ===========================================================
  # Single-target priority
  # Guide: Berserk + TF together; maintain Rake/Rip; spend at 5 CPs
  # Panther's Guile procs bonus CPs from Shred
  # ===========================================================
  st:
  # Stealth opener - exploit Prowl bonus Rake
  - rake,if=buff.prowl.up

  # Sudden Ambush: stealthed Rake is huge damage — use immediately
  - rake,if=var.sudden_ambush_up

  # Cooldowns: Berserk pairs with TF; fire both together
  # TF first if energy is low (guide: be at low energy when TF comes up)
  - tigers_fury,if=config.use_cooldowns&!var.tigers_fury_up&var.energy_low
  - berserk,if=config.use_cooldowns&var.tigers_fury_up&(target.time_to_die>15|target.boss)
  - incarnation_avatar_of_ashamane,if=config.use_cooldowns&talent.incarnation_avatar_of_ashamane&var.tigers_fury_up&(target.time_to_die>15|target.boss)
  # TF on CD even without Berserk pairing
  - tigers_fury,if=config.use_cooldowns&!var.tigers_fury_up

  # DoT maintenance - Rake: refresh early if TF is active (snapshot upgrade)
  - rake,if=var.tigers_fury_up&dot.rake.remains<9
  - rake,if=var.rake_refreshable

  # Rip: 5 CPs; ideal with TF active for snapshot
  # With Sabertooth: FB maintains Rip — only manual Rip if falling off or down
  - rip,if=var.cp_high&var.tigers_fury_up&(var.rip_pandemic|dot.rip.down)&!var.has_sabertooth&target.time_to_die>8
  - rip,if=var.cp_high&var.rip_pandemic&!var.has_sabertooth&target.time_to_die>8
  # Sabertooth fallback: only Rip when dangerously low or down
  - rip,if=var.cp_high&var.has_sabertooth&(dot.rip.remains<4|dot.rip.down)&target.time_to_die>8

  # Chomp on CD; guide says deplete energy first for the 2s grace window
  - chomp,if=talent.chomp&var.energy_low

  # Feral Frenzy / Frantic Frenzy: on CD at low CPs
  - feral_frenzy,if=talent.feral_frenzy&config.use_cooldowns&var.cp_low
  - frantic_frenzy,if=talent.frantic_frenzy&config.use_cooldowns&var.cp_low

  # Convoke: inside Berserk+TF window for maximum value
  - convoke_the_spirits,if=talent.convoke_the_spirits&config.use_cooldowns&var.berserk_up&var.tigers_fury_up&target.time_to_die>4

  # Ferocious Bite: 5 CPs; pool energy for bonus damage
  # With Sabertooth: FB extends Rip, so always safe to bite; without: don't bite if Rip is refreshable
  - ferocious_bite,if=var.cp_high&(var.has_sabertooth|!var.rip_pandemic)&var.energy_fb
  - ravage,if=var.cp_high&(var.has_sabertooth|!var.rip_pandemic)&var.energy_fb
  # Apex Predator proc (free instant Ferocious Bite — no energy needed)
  - ferocious_bite,if=var.apex_predator_up
  - ravage,if=var.apex_predator_up

  # Moonfire: maintain if Lunar Inspiration talented
  - moonfire,if=talent.lunar_inspiration&dot.moonfire.refreshable

  # Shred: primary CP builder; proc Panther's Guile
  - shred

  # ===========================================================
  # AoE priority (3+ targets)
  # Primal Wrath replaces Rip; Swipe/Rake for CP generation
  # ===========================================================
  aoe:
  # Stealth opener bonus Rake on pull
  - rake,if=buff.prowl.up

  # Sudden Ambush: stealthed Rake — high damage, use immediately
  - rake,if=var.sudden_ambush_up

  # Cooldowns: Berserk + TF together
  - tigers_fury,if=config.use_cooldowns&!var.tigers_fury_up&var.energy_low
  - berserk,if=config.use_cooldowns&var.tigers_fury_up&(target.time_to_die>15|target.boss)
  - incarnation_avatar_of_ashamane,if=config.use_cooldowns&talent.incarnation_avatar_of_ashamane&var.tigers_fury_up&(target.time_to_die>15|target.boss)
  - tigers_fury,if=config.use_cooldowns&!var.tigers_fury_up

  # Primal Wrath: maintain Rip on all targets; upgrade snapshot if TF active
  - primal_wrath,if=var.cp_high&var.tigers_fury_up
  - primal_wrath,if=var.cp_high&dot.rip.remains<5

  # Convoke: confirm Rip will outlast the full 3s channel
  - convoke_the_spirits,if=talent.convoke_the_spirits&config.use_cooldowns&var.berserk_up&var.tigers_fury_up&dot.rip.remains>4&target.time_to_die>4

  # Frenzy at low CPs
  - feral_frenzy,if=talent.feral_frenzy&config.use_cooldowns&var.cp_low
  - frantic_frenzy,if=talent.frantic_frenzy&config.use_cooldowns&var.cp_low

  # Ferocious Bite: 5 CPs if Rip healthy (5+ seconds safe); pool energy for bonus damage
  - ferocious_bite,if=var.cp_high&dot.rip.remains>5&var.energy_fb
  - ravage,if=var.cp_high&dot.rip.remains>5&var.energy_fb
  # Apex Predator proc (free — no energy needed)
  - ferocious_bite,if=var.apex_predator_up
  - ravage,if=var.apex_predator_up

  # Chomp at low energy
  - chomp,if=talent.chomp&var.energy_low

  # Rake: spread with Double-Clawed Rake
  - rake,cycle_targets=1,if=talent.double_clawed_rake&dot.rake.refreshable
  # Rake at low target count without DCR (guide: up to ~4 targets)
  - rake,if=!talent.double_clawed_rake&active_enemies<=4&dot.rake.refreshable

  # Swipe: primary AoE CP builder
  - swipe

  # Shred: low CPs with Panther's Guile on priority target
  - shred,if=talent.panthers_guile&var.cp_low

  # Moonfire on priority targets
  - moonfire,cycle_targets=1,if=talent.lunar_inspiration&dot.moonfire.refreshable

  fallback:
  - shred,if=target.exists&target.enemy&target.alive,ignore_queue=true

  main:
  - call_action_list,name=spell_queue
  - call_action_list,name=sanity_checks
  - call_action_list,name=auto_target
  - mark_of_the_wild,range_check=none,if=!buff.mark_of_the_wild.up
  - cat_form,range_check=none,if=!buff.cat_form.up
  # Combat check
  - return,if=!player.combat&!((config.auto_combat.has(1)&target.quest_mob&target.enemy)|(config.auto_combat.has(2)&target.combat&target.enemy)|(config.auto_combat.has(3)&player.auto_attacking&target.enemy))
  - return,if=!player.combat&config.auto_combat.has(0)
  # Interrupts (before cast guard so off-GCD interrupts fire while casting)
  - call_action_list,name=interrupts,if=config.use_interrupts
  # Defensives
  - call_action_list,name=defensives,if=config.use_defensives
  - call_action_list,name=dispel,if=config.use_dispel
  # Target check
  - return,if=!target.valid&!((config.auto_combat.has(1)&target.quest_mob&target.enemy&!target.dead)|(config.auto_combat.has(2)&target.combat&target.enemy&!target.dead)|(config.auto_combat.has(3)&target.targeting_party&target.enemy&!target.dead))
  - return,if=!target.valid&config.auto_combat.has(0)
  - return,if=player.casting|player.channeling
  - auto_attack
  - call_action_list,name=racials,if=config.use_cooldowns&state.cds
  - call_action_list,name=items,if=config.use_trinkets|config.use_potions
  - call_action_list,name=aoe,if=var.want_aoe
  - call_action_list,name=st
  - call_action_list,name=fallback
